<nav class="navbar navbar-expand-lg navbar-dark bg-dark p-0">
    <div class="container-fluid p-0">
        <!-- Logo -->
        <a class="navbar-brand" href="#">
            <img src="{{ asset('img/logo_profil.jpg') }}" alt="logo admin" height="50" width="auto">
        </a>

        <!-- Navbar toggler (menu burger) -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" 
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar content -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <!-- Profile -->
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_profil') }}">
                        <h6 class="navbar-user-title">
                            {% trans %}Your Profile{% endtrans %}
                        </h6>
                    </a>
                </li>
                <!-- Posts -->
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_post_index') }}">
                        <h6 class="navbar-user-title">
                            {% trans %}Your Posts{% endtrans %}
                        </h6>
                    </a>
                </li>
                <!-- Comments -->
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_comment_index') }}">
                        <h6 class="navbar-user-title">
                            {% trans %}Your Comments{% endtrans %}
                        </h6>
                    </a>
                </li>
                <!-- Messages -->
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_contact_index') }}">
                        <h6 class="navbar-user-title">
                            {% trans %}Your Messages{% endtrans %}
                        </h6>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
